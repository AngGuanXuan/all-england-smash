generator client {
  provider   = "prisma-client-js"
  engineType = "library"
}

datasource db {
  provider = "mongodb"
}

model Match {
  id          String   @id @map("_id")
  aiScore     BigInt
  playerScore BigInt
  timestamp   DateTime @db.Date
  winner      String
}
